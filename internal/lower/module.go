package lower

// lowerModule is the real pipeline; implemented in the next increment.
// Committed first with the per-ban fixtures RED (red-path-first rule).
func lowerModule(source string, ctx ModuleContext) Result {
	return Result{}
}
